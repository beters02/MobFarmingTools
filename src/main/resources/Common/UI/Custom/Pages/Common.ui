// Common UI definitions for Mob Fan upgrade page

@DefaultTooltipBackground = (TexturePath: "InGame/Tooltips/ItemTooltipDefault.png", Border: 24);
@DefaultTextTooltipStyle = TextTooltipStyle(
  Background: @DefaultTooltipBackground,
  MaxWidth: 400,
  LabelStyle: (Wrap: true, FontSize: 14),
  Padding: 20,
  ShowDelay: 0
);

@PageOverlay = Group {
  Background: (Color: #000000(0.85));
  LayoutMode: Middle;
};

@Container = Group {
  Background: (Color: #0d1117(0.95));
  LayoutMode: Top;
  Padding: (Full: 15);
};

@Title = Label {
  Style: (
    FontSize: 24,
    TextColor: #e0e8f0,
    RenderBold: true,
    HorizontalAlignment: Start,
    VerticalAlignment: Center
  );
  Anchor: (Height: 40, Bottom: 10);
};

@BackButton = Button {
  Anchor: (Width: 32, Height: 32);
  Style: (
    Default: (Background: (Color: #3a2a2a)),
    Hovered: (Background: (Color: #5a3a3a)),
    Pressed: (Background: (Color: #2a1a1a))
  );
  
  Label {
    Text: "X";
    Style: (
      FontSize: 18,
      TextColor: #c0c0c0,
      RenderBold: true,
      HorizontalAlignment: Center,
      VerticalAlignment: Center
    );
  }
};

@StatLine = Group {
    Visible: false;
    Anchor: (Height: 18, Bottom: 2);
    LayoutMode: Left;
    Label #StatDesc { Text: ""; Style: (FontSize: 12, TextColor: #b8c7d9, HorizontalAlignment: Start, VerticalAlignment: Center); }
    Group { FlexWeight: 1; }
    Label #StatValue { Text: ""; Anchor: (Height: 18); Style: (FontSize: 12, TextColor: #ffffff, HorizontalAlignment: End, VerticalAlignment: Center, RenderBold: true); }
}